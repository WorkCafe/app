<div class="page-header">
  <div class="row">
    <div class="col col-md-6">
      <h4>
        Available Cities
        <small>
          {{searchResults.length}} found.
        </small>
      </h4>
    </div>
    <div class="col col-md-6">
      {{input type='search' value=searchValue class='cities-search form-control' placeholder='Search...'}}
    </div>
  </div>
</div>

<table class="cities-list table table-hover">
  <thead>
    <tr>
      <th>City</th>
      <th>Country</th>
      <th>Continent</th>
      <th></th>
    </tr>
  </thead>
  <tbody>
  {{#each city in searchResults}}
  <tr>
    <td>{{city.city}}</td>
    <td>{{city.country}}</td>
    <td>{{city.continent}}</td>
    <td>
      {{#link-to 'cities.show' city}}
      View venues
      {{/link-to}}
    </td>
  </tr>
  {{/each}}
  </tbody>
</table>
